console.log('Node.js - легке та ефективне середовище виконання JavaScript.Дозволяє писати високопродуктивні серверні застосунки та інструменти.Node.js побудована на JavaScript - рушію V8 і написана на C++.')
// Спочатку Node.js створювалася як серверне оточення для застосунків, але розробники почали використовувати її для створення інструментів, які допомагають автоматизувати виконання локальних завдань. В результаті нова екосистема інструментів, яка виникла навколо Node.js, призвела до трансформації процесу фронтенд-розробки.
console.log('Встановлення - на оф сайті')
console.log('Перевірити версію node --version')

console.log('Пакети')
console.log('express - цей пакет для сервера')
console.log('nodemon - цей пакет дозволяє перезавандажувати локалхост автоматично, не пишучи вручну команди')
console.log('щоб перезапустити потрібно написати команду npx nodemon server/index.js (де server/index.js шлях до файлу та сам файл, що потрібно запустити')



// npm <command>

// Usage:

// npm install        install all the dependencies in your project
// npm install <foo>  add the <foo> dependency to your project
// npm test           run this project's tests
// npm run <foo>      run the script named <foo>
// npm <command> -h   quick help on <command>
// npm -l             display usage info for all commands
// npm help <term>    search for help on <term> (in a browser)
// npm help npm       more involved overview (in a browser)

// All commands:

//     access, adduser, audit, bin, bugs, cache, ci, completion,
//     shrinkwrap, star, stars, start, stop, team, test, token,
//     uninstall, unpublish, unstar, update, version, view, whoami

// Specify configs in the ini-formatted file:
//     C:\Users\User\.npmrc
// or on the command line via: npm <command> --key=value



console.log('')